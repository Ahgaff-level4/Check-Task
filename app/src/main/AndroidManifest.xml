<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyGoals.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.MyGoals.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
<!--        <activity-->
<!--            android:name=".folder.FolderListFragment"-->
<!--            android:exported="true"-->
<!--            android:label="@string/title_activity_folder_list"-->
<!--            android:parentActivityName=".MainActivity"-->
<!--            android:theme="@style/Theme.MyGoals.NoActionBar">-->
<!--            &lt;!&ndash; ParentActivity meta-data to support 4.0 and lower &ndash;&gt;-->
<!--            <meta-data-->
<!--                android:name="android.support.PARENT_ACTIVITY"-->
<!--                android:value=".MainActivity" />-->
<!--        </activity>-->
        <activity
            android:name=".file.FileListActivity"
            android:exported="false"
            android:label="@string/title_activity_file_list"
            android:theme="@style/Theme.MyGoals.NoActionBar"
            android:parentActivityName=".folder.FolderListFragment">
            <!-- ParentActivity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".folder.FolderListFragment" />
        </activity>
        <activity
            android:name=".task.TaskListFragment"
            android:exported="true"
            android:label="@string/title_activity_task_list"
            android:theme="@style/Theme.MyGoals.NoActionBar"
            android:parentActivityName=".file.FileListActivity" >
            <!-- ParentActivity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".file.FileListActivity" />
        </activity>
    </application>

</manifest>